


<div class="mt-4 mb-4">
  <a href="#" class="mt-2 btn btn-info" data-toggle="modal" data-target="modalForm">My Interests</a>


</div>

<!-- Modal for interest cards -->
<div class="modal fade" id="modalForm" tabindex="-1" role="dialog" aria-labelledby="myModalLabel"
  aria-hidden="true">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header text-center">
        <h4 class="modal-title w-100 ">Select your interests</h4>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body mx-3">
        <div class="p-2">
<!--           <%= simple_form_for(@user, url: update_interests_path(@user)) do |f| %>
            <div>
              <%= f.association :interests,
                  as: :check_boxes,
                  prompt: "Select one or more interests",
                  collection_wrapper_tag: 'div',
                  collection_wrapper_class: 'category-wrapper',
                  item_wrapper_class: 'category-item',
                  input_html: {class: 'category-selector'},
                  collection: Interest.order(:name)
              %>
            </div>
            <div class="d-flex justify-content-center">
              <%= f.submit "Submit Feedback", class: "btn btn-primary mt-2" %>
            </div>
          <% end %> -->
        </div>
      </div>
    </div>
  </div>
</div>
